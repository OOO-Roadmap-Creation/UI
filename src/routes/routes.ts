import { USER_ROLES } from '../lib/utils/constants';
import RegistrationForm from '../modules/registration-form-module/containers/registration-form'

const routesMap = {
    '/registration': {
        component: RegistrationForm
    }
};

export default routesMap
